export const filterSelector = (state) => state.filter;
export const cardSelector = (state) => state.card;
export const boardsSelector = (state) => state.board;
export const tagsSelector = (state) => state.tag;
